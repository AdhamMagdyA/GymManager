import 'package:flutter/material.dart';

class CoachQuestions extends StatelessWidget {
  const CoachQuestions({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
