import 'package:flutter/material.dart';

class NutritionistQuestions extends StatelessWidget {
  const NutritionistQuestions({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
